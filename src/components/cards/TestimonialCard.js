import { Grid } from "@mui/material";
import React from "react";

const testimonialData = [{ id: 1, name }];

export default function TestimonialCard() {
  return (
    <Grid container>
      <Grid item></Grid>
    </Grid>
  );
}
